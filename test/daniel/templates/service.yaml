apiVersion: v1
kind: Service
metadata:
  name: {{ include "daniel.fullname" . }}
  labels:
    {{- include "daniel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daniel.selectorLabels" . | nindent 4 }}
